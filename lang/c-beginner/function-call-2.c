#include <stdio.h>

int main() {
    int a = 2;
    int b = 3;
    int c = 4;

    if (a % 2 == 0) {
        printf("juft son: %d\n", a);
    } else {
        printf("toq son: %d\n", a);
    }

    if (b % 2 == 0) {
        printf("juft son: %d\n", b);
    } else {
        printf("toq son: %d\n", b);
    }

    if (c % 2 == 0) {
        printf("juft son: %d\n", c);
    } else {
        printf("toq son: %d\n", c);
    }

    return 0;
}
